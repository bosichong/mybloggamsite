---
import BaseLayout from '../layouts/BaseLayout.astro';
import { blogAggregators, blogs, extraBloggers } from '../config/site';
---

<BaseLayout title="博客圈子">
  <h1 class="page-title">✦ 博客圈子</h1>
  <p class="page-subtitle">收藏优质独立博客、博客聚合平台与游戏群演博主</p>

  <div class="nes-container is-dark">
    <div class="circle-intro">
      <p>这里收录了优质独立博客、博客聚合平台，以及在《我的博客》游戏中登场的群演博主。</p>
      <p style="margin-top: 0.5rem; color: var(--text-muted); font-size: 0.85rem;">
        共收录 <strong class="nes-text is-primary">{blogs.length}</strong> 个独立博客 ·
        <strong class="nes-text is-warning">{blogAggregators.length}</strong> 个聚合平台 ·
        <strong class="nes-text is-error">{extraBloggers.length}</strong> 位群演博主
      </p>
    </div>
  </div>

  <h2 class="section-title" style="margin-top: 2rem;">
    <span class="text-accent">■</span> 群演博客
  </h2>
  <p class="section-subtitle">在《我的博客》游戏中「博客江湖」联盟登场的博主们 ——「每一个独立博客，都是一个独一无二的世界。」向群演博主们表示深深的感谢！🙏 <a href="https://www.suiyan.cc/blog/20260717112354" target="_blank" style="color:var(--accent);">我要报名群演 →</a></p>
  <div class="extra-grid">
    {extraBloggers.map((b) => (
      <a href={b.url} target="_blank" class="extra-card">
        <div class="extra-blog">{b.blog}</div>
        <div class="extra-meta">
          <span class="extra-author">@{b.author}</span>
          <span class:list={['extra-type', b.type === '编程' ? 'type-code' : b.type === '文学' ? 'type-lit' : 'type-mix']}>{b.type}</span>
          <span class="extra-lv">Lv.{b.lv}</span>
        </div>
      </a>
    ))}
  </div>

  <h2 class="section-title" style="margin-top: 2rem;">
    <span class="text-accent">■</span> 独立博客
  </h2>
  <p class="section-subtitle">值得反复阅读的个人思考空间，如果你也有优质独立博客推荐，欢迎<a href="https://github.com/bosichong/MyBlogGame" target="_blank">联系我</a></p>
  <div class="blog-grid">
    {blogs.map((blog) => (
      <div class="nes-container is-dark blog-card">
        <a href={blog.url} target="_blank" class="blog-link">
          <h3 class="blog-name">{blog.name}</h3>
          <p class="blog-desc">{blog.desc}</p>
          <span class="blog-url">{blog.url}</span>
        </a>
      </div>
    ))}
  </div>

  <h2 class="section-title" style="margin-top: 2rem;">
    <span class="text-accent">■</span> 博客聚合平台
  </h2>
  <p class="section-subtitle">发现更多独立博客的入口，如果你也希望自己的博客聚合平台出现在这里，欢迎<a href="https://github.com/bosichong/MyBlogGame" target="_blank">联系我</a></p>
  <div class="aggregator-grid">
    {blogAggregators.map((a) => (
      <div class="nes-container is-dark aggregator-card">
        <a href={a.url} target="_blank" class="aggregator-link">
          <h3 class="aggregator-name">{a.name}</h3>
          <p class="aggregator-desc">{a.desc}</p>
          <span class="aggregator-url">{a.url}</span>
        </a>
      </div>
    ))}
  </div>

  <div class="nes-container is-dark" style="margin-top: 1.5rem;">
    <div class="submit-section">
      <p class="nes-text is-primary" style="font-family: 'Press Start 2P', monospace; font-size: 0.65rem;">
        📡 推荐博客
      </p>
      <p style="font-size: 0.85rem; color: var(--text-muted); margin-top: 0.5rem;">
        如果你有推荐的优质独立博客，欢迎在 GitHub 提交 Issue 或 PR。
      </p>
      <a href="https://github.com/bosichong/MyBlogGame" target="_blank" class="nes-btn" style="margin-top: 0.8rem;">
        推荐博客
      </a>
    </div>
  </div>
</BaseLayout>

<style>
  .circle-intro p {
    line-height: 1.6;
  }

  .circle-intro a {
    color: var(--accent);
  }

  .section-subtitle {
    color: var(--text-muted);
    font-size: 0.8rem;
    margin-top: -1rem;
    margin-bottom: 1rem;
  }

  .aggregator-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 1fr;
    gap: 1rem;
  }

  .aggregator-card {
    padding: 1rem;
  }

  .aggregator-link {
    text-decoration: none;
    display: block;
  }

  .aggregator-name {
    font-family: 'Press Start 2P', monospace;
    font-size: 0.8rem;
    color: var(--accent);
    margin-bottom: 0.4rem;
    line-height: 1.3;
  }

  .aggregator-desc {
    font-size: 0.9rem;
    color: var(--text-light);
    line-height: 1.5;
    margin-bottom: 0.4rem;
  }

  .aggregator-url {
    font-size: 0.7rem;
    color: var(--text-muted);
    word-break: break-all;
    display: block;
  }

  .blog-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 1rem;
  }

  .blog-card {
    padding: 1rem;
  }

  .blog-link {
    text-decoration: none;
    display: block;
  }

  .blog-name {
    font-family: 'Press Start 2P', monospace;
    font-size: 0.85rem;
    color: #fff;
    margin-bottom: 0.4rem;
    line-height: 1.3;
  }

  .blog-desc {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.5;
    margin-bottom: 0.3rem;
  }

  .blog-url {
    font-size: 0.7rem;
    color: #555;
    word-break: break-all;
    display: block;
  }

  .submit-section {
    text-align: center;
    padding: 1rem;
  }

  .extra-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0.6rem;
    margin-bottom: 1.5rem;
  }

  .extra-card {
    background: var(--bg-darker);
    border: 1px solid var(--border-color);
    padding: 0.5rem 0.7rem;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .extra-card {
    text-decoration: none;
  }

  .extra-blog {
    font-family: 'Press Start 2P', monospace;
    font-size: 0.55rem;
    color: #fff;
    line-height: 1.3;
  }

  .extra-meta {
    display: flex;
    gap: 0.3rem;
    align-items: center;
    font-size: 0.7rem;
    flex-wrap: wrap;
  }

  .extra-author {
    color: var(--accent);
  }

  .extra-type {
    padding: 0.05rem 0.3rem;
    font-size: 0.6rem;
  }

  .type-code {
    background: #0f3460;
    color: #88d0ff;
  }

  .type-lit {
    background: #3a1a1a;
    color: #ff9e9e;
  }

  .type-mix {
    background: #2a3a1a;
    color: #a0e0a0;
  }

  .extra-lv {
    color: var(--text-muted);
    font-family: 'Press Start 2P', monospace;
    font-size: 0.5rem;
  }

  @media (max-width: 768px) {
    .aggregator-grid {
      grid-template-columns: 1fr 1fr;
    }

    .extra-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 640px) {
    .blog-grid,
    .aggregator-grid {
      grid-template-columns: 1fr;
    }

    .extra-grid {
      grid-template-columns: 1fr;
    }
  }
</style>


